import { Button } from "@/components/ui/button";

async function Page() {
  // function addComent(comment: any) {
  //   if (comment == null) {
  //     throw new Error("example error : an error occuretded");
  //   }
  // }

  return (
    <>
      {/* <Button onAbort={() => addComent("error")}>On error trigger</Button> */}
      <h1>DASHBOARD</h1>
    </>
  );
}

export default Page;
